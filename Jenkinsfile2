pipeline {

agent any

tools {
mvn 'MAVEN_HOME'
}	

	stages {
				stage('test') {
								environment {
									mydockehub = credentials('DOKCER_HUB_PASSWORD')
								}
								
								
								
									steps {
										sh 'mvn clean'
										
										echo 'mydockehub = ${env.mydockehub}'
									}
				}
	}
			
			
}
