def myVariable = "foo"

// Print variable
pipeline {
  agent any
  stages {
    stage ("Print variable") {
      steps {
      def myVariable1 = "foo"
        echo "My variable is ${myVariable}"
      }
    }
  }
}
