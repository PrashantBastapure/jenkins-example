def myVariable = "foo"
// Print variable
pipeline {
  agent any
  stages {
    stage ("Print variable") {
      def myVariables = "foo1"
      steps {       
        echo "My variable is ${myVariable}"
      }
    }
  }
}
